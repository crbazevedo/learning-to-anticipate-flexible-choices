---
unit: W21-2
wave: W21
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Cross-check L (NDS algorithm) — closed cleanly via code-read audit
  vs legacy-cpp-v2/. Both implementations are textbook Deb-2002 fast
  non-dominated sort with bit-identical bare Pareto dominance logic.

  Constraint layer diverges intentionally: v2 uses epsilon_feasibility
  (paper Eq 7); Python uses max_cardinality (thesis §7.2.3 p.146).
  Both thesis/paper-faithful for their scopes.

  Receipt at docs/CROSS-VALIDATION-L-NDS.md.
what_broke: |
  No bugs surfaced. The bare Pareto dominance + the NDS algorithmic
  structure agree bit-for-bit. The constraint divergence is documented
  intentional, not a defect.

  Note: this is a code-read audit (no driver execution) — full
  fixture-driven driver runs were not built since the agreement is
  visible by inspection. Per W19-3 methodology (operator correction:
  "we should be careful in validating implementation that is not
  actually being used"), the code-read here references the EXACT
  production functions used by both engines.
what_youd_change: |
  W21-3 next (cross-check M — mutation operator). Then W21-4
  (cross-check N — SBX vs uniform crossover, where W15-2 already
  switched Python from SBX to uniform per thesis §7.2.3 p.147).
assumption_to_challenge: |
  Assumed NDS would be a clean AGREE based on prior W18 + W19
  audits showing both engines implement standard textbook
  algorithms for deterministic operators. CONFIRMED.

  The mutual-skepticism caveat was applied: spot-checked v2 for
  N=1 boundary handling, front-iteration termination, and duplicate-
  objective behavior. All correct.

  This closes operator check L. With W21-3 + W21-4, 14 of 14
  cross-checks will be fully closed (W18: A/B/F; W19: C/D/E/G;
  W20: H/I/J/K; W21: L/M/N).

  New carries: none — clean closure.
---

# W21-2 — cross-check L (NDS algorithm)

NDS algorithm: AGREE bit-equality on bare Pareto dominance + identical
textbook Deb-2002 structure. Constraint layer intentionally diverges
(v2 epsilon-feasibility per paper; Python cardinality per thesis).
No new bugs.
