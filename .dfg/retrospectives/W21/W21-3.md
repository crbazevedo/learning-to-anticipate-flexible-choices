---
unit: W21-3
wave: W21
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Cross-check M (mutation operator) — closed via code-read audit. Python's
  thesis_dual_mode_mutation (W15-2) is verbatim per thesis §7.2.3 p.147
  (2 modes: modify-weight + add/remove-asset).

  v2 implements FOUR mutation operators via roulette-wheel:
  modify_investment + modify_portfolio (matching the 2 thesis modes) +
  raise_entropy + lower_entropy (NOT in thesis text).

  Receipt at docs/CROSS-VALIDATION-M-MUTATION.md.
what_broke: |
  Mutual-skepticism finding: v2 has two entropy operators
  (raise_entropy, lower_entropy) that don't appear in the thesis text.
  These are paper-companion additions. Without thesis re-read of
  mutation semantics (deferred), we can't adjudicate whether they're
  intentional or vestigial. Python is thesis-faithful by default.

  No new bugs — divergence is structural (different operator suites),
  not a defect.
what_youd_change: |
  W21-4 next (cross-check N — SBX vs uniform crossover). W21-5 ablation
  should test adding v2's entropy operators to Python as one variant —
  their presence may interact with Reading E + Reading F in the
  saturation regime.
assumption_to_challenge: |
  Assumed mutation operator would be a clean divergence, since W15-2
  (cited in operator's W21 carry-forward note) had already switched
  Python from per-element to dual-mode per thesis. CONFIRMED that
  Python is thesis-faithful.

  The mutual-skepticism caveat surfaced a v2 OVER-implementation
  (2 thesis modes + 2 entropy operators not in thesis) rather than a
  Python under-implementation. This is a new finding that should feed
  W21-5 ablation: "does adding entropy ops to Python close the gap?"

  Closes (operator directive): 13 of 14 cross-checks complete after
  W21-3. W21-4 closes N to reach 14 of 14.

  New carries: W21-5 should test entropy-operator variant in the
  ablation matrix.
---

# W21-3 — cross-check M (mutation operator)

Mutation operator: STRUCTURAL DIVERGENCE — Python implements thesis
§7.2.3 p.147 verbatim (2 modes); v2 has the 2 thesis modes PLUS
2 entropy operators (raise/lower) not in thesis. Mutual-skepticism
caveat: v2 may have over-implemented; W21-5 ablation should test
adding entropy ops to Python.
