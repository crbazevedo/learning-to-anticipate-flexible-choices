---
unit: W21-4
wave: W21
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Cross-check N (SBX vs uniform crossover) — closed via code-read. The
  W18-derived backlog framing "C++ uses SBX" was WRONG. Direct read of
  legacy-cpp-v2/source/crossover_operators.cpp:13 shows v2's only
  crossover operator is `uniform_crossover` — there is no SBX in v2.

  Both engines are uniform crossover in production. Numerical divergence
  in cardinality-enforcement details (v2 = capacity-truncation during
  swap; Python = post-hoc simplex projection) but semantically equivalent.

  Receipt at docs/CROSS-VALIDATION-N-CROSSOVER.md.
what_broke: |
  The W18 backlog wording + W19-gate carry-forward note both claimed
  "C++ uses SBX, Python uses uniform" — an UNVERIFIED assumption that
  propagated through 2 waves before W21-4's direct code-read corrected it.

  This is the SECOND fabrication-pattern surfaced in W21:
  - W21-pre-correction: W20-5 fabricated a stability formula at
    portfolio.cpp:595 (no such file content)
  - W21-4 (this): "v2 uses SBX" propagated from W18 without verification

  Mitigation: per the ADR-019 §Amendment-4 analogue (cite + verify
  source-code references), every cross-check from W21+ MUST directly
  read the cited file line before claiming a divergence.

  No new code bugs — just documentation scars.
what_youd_change: |
  W21-1 (Reading-F smoke) finishing in background. After that:
  - W21-5 full ablation matrix + 30-seed run (KEYSTONE)
  - W21-6 synthesis + publication-track decision

  Going forward: every code-read citation must be verified before
  publication. The mutual-skepticism caveat applies to BOTH the
  reference and the audit chain itself.
assumption_to_challenge: |
  Assumed "SBX vs uniform" framing was already established and reliable
  (cited 2 waves running). CHALLENGED + REVISED via direct code-read.

  Underlying meta-pattern: my audit chain accumulates assumptions that
  feel "well-grounded" because they've been repeated, even when never
  empirically verified. The W20-5 stability-formula fabrication and
  this SBX-misattribution are the SAME failure mode — anchoring on
  prior-stated claims without re-verifying.

  Closes (operator directive): **14 of 14 cross-checks (A through N)
  fully complete**. W21-2/3/4 close L/M/N respectively.

  New carries: discipline upgrade — code-read citations need direct
  verification (ADR-019 §Amendment-4 analogue for source-code refs).
---

# W21-4 — cross-check N (SBX vs uniform crossover)

Crossover: BOTH engines use uniform_crossover. The "C++ uses SBX"
framing was a stale assumption propagated through W18→W19→W20 without
verification — corrected here via direct read of
legacy-cpp-v2/source/crossover_operators.cpp:13.

Numerical divergence in cardinality enforcement (capacity-truncation
vs simplex projection) but no SBX vs uniform divergence — both uniform.

14 of 14 operator cross-checks (A through N) now FULLY CLOSED.
