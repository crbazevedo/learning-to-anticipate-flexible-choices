---
unit: W3-6
wave: W3
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  `uv lock` → 35 packages resolved, uv.lock generated (347 KB).
  Removed `uv.lock` from .gitignore (was W2-4's deferred guard).
  Reproducible installs now possible via `uv sync --frozen`.
what_didnt: |
  None.
what_to_change: |
  Next packaging unit could add a CI workflow that runs `uv sync
  --frozen` to verify the lock stays current.
new_carries: |
  None.
scars_carried_forward: |
  All W3 carries unchanged at this unit's level.
---

# W3-6 — uv.lock add

Closes W2-4-CARRY-2.
