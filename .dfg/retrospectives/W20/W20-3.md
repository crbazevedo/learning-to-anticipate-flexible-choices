---
unit: W20-3
wave: W20
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Code-read audit of v2 vs Python anticipatory_learning_obj_space
  surfaced a CRITICAL nuance: v2's state-update rate is the
  TRADITIONAL accuracy+uncertainty formula (line 664), NOT the
  simple `1 - non_dominance_probability` (line 44). The line-44 α
  feeds the traditional rate as the uncertainty_factor.

  This REFINES the Reading-E story: v2 uses
    `anticipation_rate = 0.25 * ((1 - tip) + accuracy)`
  not the simpler `1 - tip` that I tested in W20-1.

  At TIP=0.5, v2's actual rate is ~0.125-0.25 (depending on accuracy),
  not 0.5 as my W20-1 flag computed. The W20-1 +7.82pp is therefore
  an UPPER-BOUND on v2's contribution.
what_broke: |
  Did NOT build a v2 execution-level driver — code-read only.
  Building a driver for anticipatory_learning_obj_space requires the
  full transaction-cost setup which is interwoven with the experiment
  driver in v2 (not easily isolated).

  Honest scar: this is a static-analysis check; the numerical
  comparison would benefit from a real driver. Filed as W21+.
what_youd_change: |
  Should have read v2's full anticipatory_learning_obj_space body
  BEFORE designing W20-1's flag implementation. The W20-1 flag is
  a simplification (over-amplification) of v2's actual rate; would
  have been more honest to test the FULL v2 formula directly.

  Code-read audits should always trace the actual call chain end-
  to-end, not just the immediately-referenced function.
assumption_to_challenge: |
  Assumed v2's `α = 1 - nd_prob` (W19-4 finding) WAS the rate used
  for state update. CHALLENGED by tracing the call chain:
   - `α` set in compute_efficiency (line 44)
   - `α` USED at line 659 as `uncertainty_factor`
   - the actual `anticipation_rate` is the traditional formula at
     line 664 that uses α as one of two factors
   - state update at line 673 uses `anticipation_rate`, not `α`

  So the Reading-E "v2 keeps anticipation alive at 0.5" finding
  is true for ALPHA (per-portfolio quantity) but the ACTUAL state-
  update rate is moderated by the accuracy factor too. v2's rate
  at TIP=0.5 is moderate (~0.125-0.25), not 0.5.

  Closes: operator check I (code-read level); execution-level
  receipt deferred.

  New carries:
   - W20-3-CARRY-1: build anticipative_distribution_driver.cpp once
     transaction-cost setup is factorable
   - W21 candidate: implement full v2 state-update formula flag
---

# W20-3 — Cross-check I OAL audit (structural divergence with refined Reading E)
