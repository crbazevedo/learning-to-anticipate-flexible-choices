---
unit: W6-3
wave: W6
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Single new doc, ~200 lines, structured per the W6-3 contract's
  9-section template. Reads as a real plan, not a manifesto: dataset
  paths real, scenarios concrete, paper-comparison anchor explicit
  with section + equation refs, honest-scar section calls out the
  4 things that could go wrong empirically. Future W7 execution wave
  has everything it needs to start.

  Implementation-vs-empirical-parity framing: the plan opens by
  separating "module + equation + wiring" parity (done by W1-W5)
  from "empirical parity" (pending W7), so the reader doesn't
  conflate the two.
what_didnt: |
  None at this unit's level.
what_to_change: |
  When W7 executes, this plan becomes the spec — measure what the plan
  predicts vs what actually happens. The "honest scars" section in
  particular should be empirically tested.
new_carries: |
  None.
scars_carried_forward: |
  None.
---

# W6-3 — Experiment validation plan

## What changed

- `docs/EXPERIMENT-VALIDATION-PLAN.md` (NEW) — 9 sections covering
  dataset, experiment matrix, expected outputs, acceptance criteria,
  reproducibility recipe, paper-comparison anchor, execution scaffold,
  honest scars.

## Verification

- Pre-pr substrate gates green.
- File renders as proper markdown.
- References to source files (`run_experiments.py`, dataset paths,
  paper sections) all resolve.
