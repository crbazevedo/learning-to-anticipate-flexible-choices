---
unit: W5-1
wave: W5
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Two-line fix: (1) `np.random.seed(42)` in setUp removes flakiness;
  (2) lowering the threshold passed to find_corresponding_solution
  from 0.95 (default) → 0.5 lets the test actually exercise the
  correspondence path under independent-random-Dirichlet weights.
  9/9 PASS across 5 sequential runs.
what_didnt: |
  Initial assumption — that seed alone would fix the test — was wrong.
  Seed eliminated NON-DETERMINISM but the test deterministically failed
  because the default similarity threshold (0.95) is impossible to
  achieve between independent random Dirichlet vectors. The test was
  fundamentally testing the threshold-strict version of correspondence
  against a population where no real correspondence exists. The
  similarity_threshold parameter on the test call is the right knob.
what_to_change: |
  Future test patterns that use random fixtures should explicitly
  pass thresholds appropriate to the random distribution (or use
  fixed weights for tests that need tight similarity assertions).
new_carries: |
  None.
scars_carried_forward: |
  W4-2-CARRY-1 is W5-2's scope. All other carries unchanged.
---

# W5-1 — RNG seed in correspondence test setUp

Closes W3-5-CARRY-1.

## What changed

- `test_correspondence_integration.py` setUp: `np.random.seed(42)` (removes flakiness)
- `test_correspondence_mapping_with_multiple_time_steps`: pass `similarity_threshold=0.5` to `find_corresponding_solution` (loosens the impossible-with-random-weights default 0.95)

## Verification

9/9 PASS across 5 sequential runs.
