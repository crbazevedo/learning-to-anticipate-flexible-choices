---
unit: W16-1
wave: W16
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  W16-1 (keystone) shipped λ^K consumption per thesis Eq 7.16 + Eq 6.9.

  Audit-first discipline (per W15-1 pattern) was load-bearing. Pre-W16
  `compute_anticipatory_learning_rate`:
   - Used `_compute_traditional_learning_rate` as the "λ^K" arm — a
     SINGLE-TIME scalar based on (prediction_error, alpha) of one
     solution. NOT the K-historical-window squared-residual sum per
     Eq 6.9.
   - Short-circuited: when `tip_rate == 0`, λ collapsed to
     `traditional_rate` alone, NOT `0.5*(0 + traditional)`. Violated
     Eq 7.16 verbatim ("balanced tension" requires the average even
     if one term is zero).
   - Did NOT accumulate KF squared residuals over the last K periods.
   - Did NOT consume `self.window_size` in the residual-sum sense.

  Fix: new `_compute_lambda_k(...)` per Eq 6.9; new
  `record_kf_residual(...)` rolling K-length buffer; replaced
  `compute_anticipatory_learning_rate(...)` so it always computes
  λ = 0.5*(λ^H + λ^K) regardless of TIP availability.

  Tests: 10 shipped (≥ 4 required). All green. 56/56 in full W15/W16
  thesis-spec suite.

  PR #89 merged.
what_broke: |
  Pre-existing test-isolation bug surfaced: Portfolio.mean_ROI is a
  CLASS variable cached across tests. My Solution(num_assets=5)
  Crashed on matmul when test_compute_transaction_cost (which exists
  on TIPIntegratedAnticipatoryLearning but the pre-existing test
  calls it on base class) ran first and left cached length-3 mean_ROI.

  Fix: added autouse fixture to reset Portfolio.mean_ROI/covariance
  per test. The class-hierarchy bug itself is W16-5-CARRY-2 (W17).

  PR-body audit echoing thesis Eq 7.16 verbatim took ~5 min; worth it.
what_youd_change: |
  Would have liked to also wire `record_kf_residual` calls into the
  walk-forward driver so the buffer actually populates during the
  W16-5 smoke. As-shipped, the buffer is only populated if the
  caller explicitly calls record_kf_residual — which neither the
  ExperimentManager nor walk_forward.py does today. So in practice
  λ^K falls back to the "warm-up traditional rate" branch, not the
  "K-period residual sum" branch.

  Filed inline-fix candidate for W17: wire record_kf_residual into
  the Kalman update path (kalman_filter.py probably owns the
  residual; needs return-value or a per-step hook).

  This is why W16-5's improvement (-8.75% → -5.53%) was likely
  mostly W16-2 (txn costs) and not W16-1 — W16-1 is structurally
  CORRECT now but its production behavior is still warm-up mode.

  Receipt: W16-5 trace assertions would have caught this immediately
  (mean(λ^K) > 0.01 for K=3 scenarios), but trace CSV emit wasn't
  wired through walk_forward_report.py (W16-5-CARRY-1).

  Honest scar but the structural fix is in place — W17 just needs
  to flip the switch on the residual-recording call site.
assumption_to_challenge: |
  Assumed: changing the formula would change production behavior.
  CHALLENGE: only the formula's preconditions (empty/non-empty
  residual buffer) gate which branch fires. Since the buffer is
  always empty in production until W17 wires record_kf_residual,
  the production λ today is still effectively "warm-up traditional
  rate". The formula is correct on paper; production behavior is
  approximately unchanged.

  The 3.22pp improvement in W16-5 vs W15-5 is then more attributable
  to W16-2 (txn costs in HV) + W16-3 (extrema preservation) than
  W16-1 per se. Trace assertions in W17 will let us decompose
  cleanly.

  Closes (BACKLOG): H2 + W15-3-CARRY-1 consumption half
   (formula correct; production behavior wiring deferred to W17)

  New carries:
   - W16-1-CARRY-1: wire record_kf_residual into Kalman update path
     so λ^K can actually fire in production (vs warm-up fallback)
---

# W16-1 — λ^K consumption per Eq 7.16 + Eq 6.9 (BACKLOG H2 + W15-3-CARRY-1)
