---
unit: W16-3
wave: W16
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  W16-3 shipped rank-1 extrema preservation in SMS-EMOA reduce step
  per thesis §3.4.2 + Beume et al. 2007 REF + STD practice.

  New `_identify_protected_anchors()` returns set of rank-1 argmax(ROI) +
  argmin(risk) population indices. Empty set when rank-1 < 3 (edge
  case to avoid reduce deadlock).

  Augmented `_remove_worst_solution()` to skip protected indices when
  picking worst HV contribution; falls back to original behavior if
  every solution is protected (small-front edge case).

  New `extrema_trace: List[Dict]` populated per-gen by _track_hypervolume:
  {gen, roi_max, roi_min, risk_max, risk_min, front_size}.

  Tests: 10 shipped (≥ 3 required). All green. 25/25 in combined W15
  thesis-spec + W16-1 + W16-3 suite.

  PR #90 merged.
what_broke: |
  None significant. The change was surgical (one new method, one
  augmented method, one new instance attribute). Tests passed first
  try.

  The W16-5 smoke's wall-clock improvement (390s vs W15-5's 858s,
  55% faster) is partially attributable to W16-3: stable front
  extent means HV computation doesn't oscillate as much, which
  reduces redundant work.
what_youd_change: |
  Could have added a "front-extent monotonicity" assertion to the
  trace test: `roi_max[t+1] >= roi_max[t]` for all t. This would
  catch regressions where extrema preservation fails. Deferred —
  the synthetic stress test (100 reduces, anchor survives) covers
  the same invariant locally.

  The trace dict uses string keys; would be more efficient as a
  NamedTuple. Premature optimization.

  Should have integrated the extrema_trace into metrics_collector's
  algorithm_metrics output so it surfaces in experiment-output dirs
  alongside other traces. Deferred to W17/W18 (when full
  instrumentation lands).
assumption_to_challenge: |
  Assumed: extrema preservation would meaningfully reduce HV
  oscillation. CHALLENGE: hard to attribute the W16-5 improvement
  to W16-3 specifically without ablation. The 55% wall-clock
  improvement is a meaningful signal but could also be W16-2's
  txn-cost penalty pulling churn-y portfolios out of the search
  earlier.

  W17 ablation should compare {W16-1, W16-2, W16-3, all} to isolate
  each effect on (gap_closure, wall_clock, front_stability).

  Closes (BACKLOG): H6 fully

  New carries:
   - W16-3-CARRY-1: surface extrema_trace in metrics_collector's
     algorithm_metrics output (W18 integration)
---

# W16-3 — Rank-1 extrema preservation in SMS-EMOA reduce (BACKLOG H6)
