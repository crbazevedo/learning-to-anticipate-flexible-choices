---
unit: W16-4
wave: W16
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  W16-4 shipped λ + TIP per-call trace CSV emit.

  AnticipatoryLearning.LAMBDA_TRACE_CSV_HEADER class const +
  flush_lambda_trace_csv(path, append=True) method. ExperimentManager
  reads data["lambda_trace_csv_path"] and flushes after algorithm.run
  (non-fatal on error). walk_forward exposes
  --lambda-trace-csv-path-equivalent kwarg through
  _train_and_extract_pareto.

  Tests: 4 shipped (≥ 2 required). All green.

  Append-mode + header-deduplication tested via two-period CSV
  read-back; periods [1, 1, 2, 2, 2] confirms ordering.

  PR #92 merged.
what_broke: |
  Type annotation `str | "os.PathLike[str]"` failed to evaluate
  at class-body load time (Python 3.11 quirk with stringified
  PathLike in union). Fixed by dropping the type hint to bare
  `path` parameter — pytest collection then proceeded.

  Caught in 1 cycle via the test failure; ~30 sec resolution.
what_youd_change: |
  Did NOT wire the CSV path through walk_forward_report.py (the
  smoke driver). As a result the W16-5 smoke could only measure
  aggregate Δ(S2−S0) and not directly assert "both λ arms fire".
  Filed as W16-5-CARRY-1.

  The flush call is non-fatal (try/except logs warning); good for
  smoke robustness but could mask bugs. Should add a
  --strict-trace flag for future smokes that REQUIRES the trace
  emit to succeed.

  Should have added a test for the "non-fatal flush failure" path —
  inject a permission error and verify the algorithm.run continues.
  Deferred — covered by the structural design (try/except), but
  not regression-tested.
assumption_to_challenge: |
  Assumed: trace export is sufficient for W16-5 verifier. CHALLENGE:
  the verifier needs the trace to actually exist on disk, which
  requires the walk_forward_report driver to pass the path. I
  shipped only one half of the plumbing.

  Should have shipped the driver-side wiring as part of W16-4 or
  blocked W16-5 on that wiring. As-shipped, W16-5 can verify the
  numeric result but not the mechanism. W17 fixes it.

  Closes (BACKLOG): M3 + M4 (partial — trace-export scope only;
  full Fig 7.4-7.13 rendering is W18)

  New carries:
   - W16-4-CARRY-1: wire --lambda-trace-csv-path through
     walk_forward_report.py (see W16-5-CARRY-1; same scope)
---

# W16-4 — λ + TIP trace CSV emit (BACKLOG M3 + M4 partial)
