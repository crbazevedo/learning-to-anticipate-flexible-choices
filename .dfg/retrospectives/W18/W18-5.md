---
unit: W18-5
wave: W18
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  W18 cross-validation foundation shipped: harness + 3 checks + synthesis.

  4 PRs (+ 1 replan):
    #104 — W18 replan
    #105 — W18-1 harness scaffold (Makefile, 4 build adapters,
            Python compare framework, 10 tests, FIRST C++ bug surfaced
            inline via -Wunused-value warning)
    #106 — W18-2 cross-check A (risk): MAJOR finding — Python adds
            sqrt() not in thesis Eq (7.4); ratio py = sqrt(cpp) exactly
    #107 — W18-3 cross-check B (ROI): AGREE within 1e-12 (machine
            precision; no new bug)
    #108 — W18-4 cross-check F (TIP): three-implementation comparison
            confirms saturation is STRUCTURAL, not code bug — Reading C
            confirmed for W17-5 framework

  19/19 W18 tests green throughout.

  Two BUGS surfaced via mutual-skepticism cross-validation:
    1. C++ portfolio.cpp:65 comma-operator bug (off-headline) — first
        finding, validates operator's caveat
    2. Python compute_risk adds sqrt() (on-headline) — W18-CARRY-1

  Strategic finding: W17-5 saturation is NOT a code bug. It's a
  structural data property. Reading C ("anticipation can't engage when
  predictive distributions are maximally uncertain") is the most
  likely diagnosis for S2 ≤ S0.
what_broke: |
  Operator wanted "investigate everything" — 14 checks. Honest scope:
  W18 delivered 3 (A risk, B ROI, F TIP); 11 remain (C, D, E, G, H,
  I, J, K, L, M, N) for W19-W21+.

  W18-4 did NOT run the actual C++ TIP binary (depends on
  pmvnorm/Fortran mvtdst_, stubbed on Apple silicon). Re-implemented
  C++ formula in Python — faithful arithmetic but not binary-level
  execution. Filed as W18-4-CARRY-1.

  W18-4 F2 sub-check (production-input replay) deferred because the
  W17-5 trace CSV doesn't carry KF covariance matrices — only
  scalar TIPs. Schema needs expansion. W18-4-CARRY-2.

  Initial W18-4 contract speculated TIP structural divergence (C++
  same-sign vs Python independent-sample MC). On numerical comparison
  they AGREE — my initial reading was wrong. Documented honestly in
  the receipt + commit message.
what_youd_change: |
  W19 priority order based on W18 evidence:

  1. W19-1 cross-check D (KF Gaussians) — Reading-C critical test.
     If C++/Python KFs disagree on predictive (mean, cov), upstream
     drift to fix. If they agree, structural-uncertainty is confirmed
     and the operator decision moves to strategic level.

  2. W19-2 cross-check C (bivariate Gaussian inputs to KF) — upstream
     of D; sets up the inputs.

  3. Operator decision on W18-CARRY-1 (Python sqrt in compute_risk):
     fix or document. Likely needs to happen before W19 because the
     risk-scale choice affects KF covariance estimates downstream.

  4. W19-3+ continue down the operator's A-N list with established
     harness pattern.

  Should have spent 15 minutes earlier mapping the C++ TIP formula
  before speculating about "structural divergence" in W18-4 contract.
  Would have saved a paragraph of incorrect framing in the contract
  body (corrected in the receipt + commit message).

  The mvtdst Fortran stub on Apple silicon means a small set of
  C++ drivers can't be directly executed. If this becomes a recurring
  blocker, a one-time Fortran toolchain install would resolve it.
  Deferred until it blocks something concrete.
assumption_to_challenge: |
  Assumed: cross-validation against C++ would surface a Python bug
  that explains the gap. CHALLENGE: it surfaced ONE Python bug
  (sqrt in compute_risk) but also confirmed via three-implementation
  comparison that the TIP saturation isn't a bug at all — it's
  structural data property.

  The 14-check directive may produce SMALL bug-finding ROI per
  remaining check. Most likely outcomes for W19-W21:
   - Cross-check D (KF): may or may not find drift
   - Cross-check C (Gaussian inputs): likely AGREE (mean/cov are
     simple)
   - Cross-checks J/K/L (selection/HV): likely AGREE (deterministic
     algorithms)
   - Cross-checks M/N (operators): possibly differ — W15-2 switched
     Python to uniform crossover; C++ uses SBX. Worth comparing.
   - Cross-check G (anticipative rate): high stakes — verifies the
     full Eq 7.16 chain end-to-end against C++

  Most informative remaining check is likely G (full λ end-to-end).
  W19 should prioritize C → D → G as the focused critical path.

  Closes (BACKLOG / operator directive):
   - 3 of 14 cross-checks complete (A, B, F)
   - Harness ready for the remaining 11
   - Major finding: TIP saturation is STRUCTURAL (Reading C)
   - Major finding: Python compute_risk has thesis deviation

  New carries:
   - W18-CARRY-1 (operator decision): Python compute_risk sqrt fix/doc
   - W18-4-CARRY-1: C++ TIP binary not executed (Apple silicon stub)
   - W18-4-CARRY-2: F2 production-replay deferred (trace schema gap)
   - W18-CARRY-2 (NEW): Reading C is most likely diagnosis; W19+
     needs to test KF predictive parity to confirm
---

# W18-5 — Cross-validation synthesis + W19+ roadmap

## What W18 delivered

| Unit | PR | Verdict |
|---|---|---|
| W18-1 | #105 | Harness scaffold + FIRST C++ bug surfaced inline |
| W18-2 | #106 | MAJOR: Python compute_risk adds sqrt() not in thesis Eq (7.4) |
| W18-3 | #107 | AGREE within machine precision |
| W18-4 | #108 | TIP saturation is STRUCTURAL, not code bug (Reading C) |
| W18-5 | THIS | Synthesis + W19-W21 roadmap |

Plus W18 replan PR #104.

## Strategic outcome

W17-5 framework revised: Reading A (wrong metric) vs B (replication failure)
extended to include **Reading C (structural data property)** — most likely
diagnosis per W18-4 three-implementation TIP agreement.

W19+ keystone: cross-check D (KF Gaussians) to confirm Reading C.
