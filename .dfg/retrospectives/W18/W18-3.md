---
unit: W18-3
wave: W18
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Cross-validation B (ROI): AGREE within machine precision. Both C++
  and Python compute μ̂^T u verbatim per Eq (7.4). rel_max = 0 over
  10 portfolios.

  Confirms returns are decimal (not %), no daily-vs-annualized drift.
  2 tests, green.
what_broke: |
  Nothing — clean AGREE outcome. Bug count unchanged after W18-3.
what_youd_change: |
  Pattern is now established: per-check driver pair (C++ + Python) +
  shared fixture + comparison. Each subsequent check (D, E, G, ...)
  is ~30 min if the function is well-isolated, longer if state-setup
  is required.
assumption_to_challenge: |
  Assumed: ROI might also have a Python deviation similar to risk.
  CHALLENGE: it doesn't. The sqrt() deviation is UNIQUE to compute_risk.
  Suggests the Python port was deliberately set up to use std-dev
  for risk (perhaps for plotting), and ROI was left at thesis verbatim.

  Closes: operator check B; no new carries
---

# W18-3 — Cross-check B (ROI)
