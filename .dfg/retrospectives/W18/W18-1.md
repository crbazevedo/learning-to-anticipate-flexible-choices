---
unit: W18-1
wave: W18
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Cross-validation harness scaffold shipped end-to-end. C++ buildable
  on Apple silicon + clang 17 + Eigen 3.4 with 4 mechanical patches
  (Eigen include path, double→uint narrowing, -std=c++14,
  -DEIGEN_DONT_VECTORIZE). 10/10 .o files compile.

  Python compare framework with mutual-skepticism reporting + scale
  ratio diagnostic. CLI wrapped.

  10/10 harness tests green.

  Filed Fortran mvtdst stub for drivers that don't need multivariate
  normal CDF; risk_driver linked clean.
what_broke: |
  FIRST C++ BUG SURFACED inline via -Wunused-value warning during
  compile: portfolio.cpp:65 uses comma operator instead of pow(),
  resulting in var += 4.0 per element. Off the headline path
  (autocorrelation diagnostic) but validates operator's
  mutual-skepticism caveat.

  Build adapter needed cwd-resolution fix during Make iteration
  (forgot to apply sed to headers as well; caught immediately on
  first compile attempt).
what_youd_change: |
  Would have liked to ship one example driver as part of W18-1 (e.g.
  a no-op driver that just reads the fixture + emits identity).
  Instead the first driver (risk_driver) shipped in W18-2 which
  proved the end-to-end pattern.

  Should have documented the Apple-silicon-specific path
  (/opt/homebrew vs /usr/include) more prominently in
  README-BUILD-ADAPTER.md.
assumption_to_challenge: |
  Assumed: legacy C++ would build cleanly with minor patches.
  CHALLENGE: needed 4 distinct adapters (include path, narrowing,
  C++ standard, vectorization). For a 2013-era 3000-LOC codebase
  on a 2026 toolchain, 4 mechanical patches is light — but each
  has a specific reason that took a few minutes to diagnose.

  Closes: W18 prerequisite for A-N checks

  New carries: none beyond W18-CARRY-2 (Fortran toolchain for
  full mvtnorm path; deferred)
---

# W18-1 — Cross-validation harness scaffold
