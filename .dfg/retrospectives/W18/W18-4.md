---
unit: W18-4
wave: W18
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Cross-check F (TIP) with three independent implementations:
   - py_mc (current Python TIP code, MC sampling)
   - cpp_same_sign (C++ analytical formula re-implemented in Python)
   - py_pareto_mixed (additional cross-check, MC over Δ-quadrants)

  All three AGREE on the production regime (TIP ≈ 0.50 within 0.05),
  REPRODUCING the W17-5-CARRY-1 RESMOKE saturation finding in
  synthetic.

  Confirms: TIP saturation is STRUCTURAL (data + KF cov property),
  not a code bug. Reading C (data-property hypothesis) added to the
  W17-5 strategic framework.

  4 tests, all green; 19/19 in W18 suite.
what_broke: |
  W18-4 contract initially speculated "C++ same-sign vs Python
  mixed-direction" structural divergence. On numerical comparison
  the implementations AGREE — my initial reading was an analysis
  error. For ROI=max + risk=min Pareto dominance, mutual non-dominance
  IS the matching-sign delta event. The contract framing was wrong
  but the receipt + commit message corrected it honestly.

  Did NOT run the actual C++ TIP binary (depends on pmvnorm/Fortran
  mvtdst_ stubbed on Apple silicon). Re-implemented C++ formula in
  pure Python — faithful arithmetic but not binary-level execution.
  W18-4-CARRY-1.

  F2 production-replay deferred because W17-5 trace doesn't carry
  KF covariance matrices. W18-4-CARRY-2.
what_youd_change: |
  Should have spent 15 min reading C++ TIP formula BEFORE drafting
  the contract — would have avoided the "structural divergence"
  red herring in the dispatch_instructions.

  The three-implementation approach (current + C++ re-impl + Pareto
  re-impl) was the right pattern — when one source has a bug, the
  third tie-breaker is invaluable.

  For W19+ TIP-adjacent checks (G anticipative rate): consider
  threading KF covariance into the trace CSV schema so future smokes
  enable F2-style replay.
assumption_to_challenge: |
  Assumed: cross-validation would surface a TIP bug. CHALLENGE: no
  TIP bug — saturation is structural data property. This is
  scientifically more interesting than a bug fix because it bounds
  what the algorithm can achieve on this data.

  Reading C is now the most likely diagnosis. W19+ should test it
  by checking whether KF predictive distributions are themselves
  identical across implementations (cross-check D), which determines
  whether structural-uncertainty is the algorithm's correct response
  to inherent data uncertainty (Reading C confirmed) or a parameter
  bug somewhere upstream (Reading C refuted, deeper fix needed).

  Closes: operator check F; W17-5 saturation question resolved

  New carries:
   - W18-4-CARRY-1: actual C++ TIP binary not executed
   - W18-4-CARRY-2: F2 production-replay deferred (trace schema)
---

# W18-4 — Cross-check F (TIP) saturation diagnosis
