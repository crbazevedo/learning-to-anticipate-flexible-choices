---
unit: W18-2
wave: W18
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Cross-validation A (risk) surfaced a MAJOR Python deviation from
  thesis Eq (7.4) on FIRST execution:

   C++:  compute_risk = u^T Σ u                (variance)
   Py:   compute_risk = sqrt(max(u^T Σ u, 0))  (std-dev)

  Ratio confirmed py = sqrt(cpp) within 1e-15 over 10 portfolios.

  3 tests shipped, all green. Receipt at
  docs/CROSS-VALIDATION-A-RISK.md with full math + impact analysis.
what_broke: |
  Even reading the code before running flagged the disagreement. The
  Python docstring openly admits "(standard deviation)" — the
  deviation is documented but not justified vs the thesis text.

  Did NOT fix the bug in W18-2 — that's W18-CARRY-1, deliberately
  punted to operator decision because the choice affects downstream
  KF residual scales (W17-5 saturation).
what_youd_change: |
  Could have caught this earlier in the wave-chain — reading the
  Python source vs thesis text would have shown the sqrt() in 5 min.
  But that's hindsight; without a structured cross-check the
  deviation hid in plain sight through 17 waves of work.

  The execution-level confirmation (numerical receipt) is the right
  output even when the code-read alone would suffice — it locks in
  the finding with a reproducible artifact.
assumption_to_challenge: |
  Assumed: code-read parity might be sufficient. CHALLENGE: numerical
  execution surfaced the EXACT sqrt relationship which makes the
  diagnosis unambiguous. Code-read alone would have said "Python
  has sqrt, thesis doesn't"; execution adds "and the ratio is exactly
  sqrt within 1e-15".

  Closes: operator check A; new W18-CARRY-1 for the fix decision

  New carries: W18-CARRY-1 (Python compute_risk sqrt — fix or doc?)
---

# W18-2 — Cross-check A (risk)
