---
unit: W19-4
wave: W19
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Cross-check G anticipative rate: THREE-WAY DIVERGENCE surfaced.
  Built pure-Python re-implementation of all 3 formulas (v1, v2,
  Python production) + TIP grid comparison. 14 tests; receipt at
  docs/CROSS-VALIDATION-G-ANTICIPATIVE-RATE.md.

  KEYSTONE finding: at TIP=0.5 (W17-5 production regime):
    v1 alpha       = 0.0   (tent peak → 1-1)
    v2 alpha       = 0.5   (monotonic; ACTIVE)
    Python λ (Eq 7.16) = 0.0   (Shannon → 1-1)

  v2 is the outlier. It keeps anticipation alive in EXACTLY the
  regime where v1 and Python both collapse to zero. v2 is the
  paper-headline-generating code. So Reading E (formula-choice
  divergence) is the strongest explanation of the W17-5 chain.
what_broke: |
  Discovered linear_entropy is a TRIANGULAR TENT function (NOT Shannon
  entropy as I initially assumed). v1's `1 - linear_entropy(p)`
  happens to behave SIMILARLY to `1 - binary_entropy(p)` (both peak at
  p=0.5) — but the tent is piecewise-linear while Shannon is concave.
  v1 ALPHA at p=0.5 is exactly 0; Python λ^H at p=0.5 is also exactly
  0. Both collapse.

  v2 chose a fundamentally different formula (1-p, monotonic) that
  does NOT collapse at p=0.5. This may have been INTENTIONAL to keep
  anticipation alive in uncertainty regimes.
what_youd_change: |
  Should have read all three statistics.cpp files (v1 + v2 + Python
  binary_entropy) in parallel before writing the contract. That would
  have surfaced the formula divergence in 15 min instead of waiting
  for the comparison table to reveal it.

  The pure-Python re-implementation pattern (vs trying to compile a
  C++ driver and run it) is the right move when the function is
  small + deterministic + arithmetic-only. Faster iteration than
  C++ build cycle.
assumption_to_challenge: |
  Assumed v1 and v2 used the SAME formula. CHALLENGED by reading both
  sources: v1 wraps nd_prob in linear_entropy (symmetric); v2 uses
  nd_prob directly (monotonic). The thesis Eq 7.16 specifies a
  different formula yet again (the 0.5*(λ^H + λ^K) average).

  Three sources, three formulas, all claiming Eq 6.6 / Eq 7.16
  ancestry. Only Python implements the thesis verbatim. v2's choice
  (whatever the motivation) is what generated the paper headline.

  Closes: operator check G with Reading E (formula divergence)
  identified as the dominant explanation for W17-5.

  New carries:
   - W19-4-CARRY-1 (NEW): add Python flag use_v2_anticipative_rate
     and re-run W17-5 smoke; if S2 > S0, paper replicates
   - W19-4-CARRY-2 (NEW): re-read thesis Eq 7.16 line-by-line for
     the (1/2) factor semantics; is it averaging or a normalization?
---

# W19-4 — Cross-check G anticipative rate: Reading E (formula divergence)
