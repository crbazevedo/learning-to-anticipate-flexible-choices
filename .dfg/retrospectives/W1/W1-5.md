---
unit: W1-5
wave: W1
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  Paper-pages-1-through-9 read before drafting the contract gave me
  the full equation inventory ahead of time. The reconciliation table
  in the contract's dispatch_instructions was authored from that
  reading + the existing audit; when I went to implement, the table
  was already truth-checked. No back-and-forth between contract and
  implementation.

  Mass-annotation via Edit replace_all=true was efficient. 25+ thesis-
  eq citations across 2 files in 12 Edit calls. The trick was
  ORDERING — range citations ("Equations 6.6-6.7") first so the
  subsequent single-eq replaces ("Equation 6.6") wouldn't double-
  annotate the range patterns. Verified by post-grep that the
  remaining matches are all properly annotated (only false-positive
  on the previously-annotated line itself).

  Honest deletion. repository_analysis.md was stale on three load-
  bearing claims (test count, "100% C++ equivalence", silent about
  the 2025-09 thesis-adherence layer). The audit had flagged it;
  this unit acted on the flag rather than letting it lurk. Git
  history preserves the file if anyone ever wants to recover it.

  Contract-scope discipline. The contract explicitly forbade touching
  context-map.yaml. When I discovered context-map.yaml has a must_read
  pointing to the soon-to-be-deleted repository_analysis.md, the
  disciplined response was: leave the dangling reference, name it as
  a scar, queue a separate housekeeping unit. Strict contract
  adherence > convenient scope creep.

what_didnt: |
  context-map.yaml has a dangling must_read reference. After deleting
  repository_analysis.md, the W1-5 must_read in .dfg/context-map.yaml
  line 48 still points to it. The reference is schema-valid (it's a
  string), so `dfg validate` passes, but the SessionStart hook will
  try and fail to inject it on any future re-dispatch of W1-5
  context (unlikely — W1-5 will be COMPLETED). Tolerable for now;
  documented as W1-5-CARRY-1.

  thesis_codebase_analysis.md still contains the "100% adherence"
  claim (line 525-ish: "Complete anticipatory learning rule").
  This is technically a paper-grounded claim now (paper Eq 14), but
  the wider claim in the doc is about "implementation completeness"
  which the audit refuted (the modules exist but aren't wired).
  This unit's scope was equation-citation reconciliation only;
  rewriting the adherence claims is a separate doc unit. Honest
  scar — the doc is more truthful than before this unit, but not
  yet fully truthful.

  The 100_percent_adherence_backlog.md is still Portuguese. PT
  translation was explicitly out of scope per the contract, but it
  means every English-speaking contributor must rely on the new
  canon note at the top + the reconciliation table in the sibling
  English doc to navigate the backlog. Acceptable carry; queued.

  Pre-pr's contract-first commit check SKIPped again with "cannot
  resolve merge-base against origin/main or main" — same scar as
  W1-1. Confirms recurrence ≥ 2. Per operator directive #5: this
  recurrence triggers "ship the dfg gate that retires the class"
  — but the directive ALSO says "don't modify the harness." The
  resolution is to surface this as feedback to dfg-harness in a
  separate channel (next dfg-harness session), not to fix it here.
  Documented as W1-5-CARRY-2 + composing-scar with W1-1-CARRY-2.

what_to_change: |
  Add a tiny "W1-5-followup" unit (or roll into the next planning
  ceremony) that updates .dfg/context-map.yaml: remove the dangling
  repository_analysis.md from W1-5's must_read; remove the now-
  superseded W1-5 entry entirely (W1-5 is now COMPLETED). Single-
  file change; trivial PR.

  Queue an EN-translation unit for 100_percent_adherence_backlog.md
  for W2 or whenever the doc surface is next on the critical path.
  Same shape as W1-5: docs-only, low-risk, file-disjoint with code
  units.

  For the contract-first-on-master scar: when dispatching the next
  unit on this repo, include in the dispatcher brief a note that
  pre-pr's contract-first SKIP is expected (master, not main) and
  P3 discipline is operator-verified by inspecting the branch's
  first commit manually. Until upstream harness retires the class,
  this is the discipline.

new_carries: |
  - W1-5-CARRY-1: .dfg/context-map.yaml W1-5 must_read points at the
    deleted repository_analysis.md. Schema-valid; hook-injection
    would warn-or-skip. Trivial follow-up to clean up.
  - W1-5-CARRY-2: pre-pr contract-first SKIPs on master-default
    (recurrence ≥ 2; composes with W1-1-CARRY-2). Resolution path
    is upstream-feedback, not local-modify.
  - W1-5-CARRY-3: thesis_codebase_analysis.md's adherence-claims
    (vs equation citations) still need a truthing pass. Separate
    unit scope.

scars_carried_forward: |
  W1-1-CARRY-2 (pre-pr contract-first on master) recurred in W1-5
  identically. Per operator directive #5: ship the dfg gate that
  retires the class in dfg-harness — NOT here. This unit does NOT
  write to dfg-harness. The recurrence is documented; resolution
  is queued as feedback for the next dfg-harness session via the
  operator-channel.

  W1-1-CARRY-1 (kit/SCHEMAS symlink required) remained operational
  through W1-5 with no friction. The .gitignored symlink approach
  continues to work; will obsolete when ADR-033 §Stage 2 lands.

  W1-1-CARRY-3 (eq-level tests don't run in CI) is unchanged — W1-5
  doesn't touch code, so this carry is unaffected. Closes when
  W1-3 lands.
---

# W1-5 — Reconcile docs to paper canon

## Headline

**25+ thesis-equation citations across 2 docs are now paper-anchored.**
A new reconciliation table at the top of `thesis_codebase_analysis.md`
maps every thesis equation to its IEEE paper equivalent (or flags
"thesis-only extension" when not in the paper). The stale
`repository_analysis.md` (273 lines, 3 falsified load-bearing claims)
is deleted; its role is filled by `docs/VISION.md`.

## What changed

| File | Change |
|---|---|
| `thesis_codebase_analysis.md` | NEW §0 reconciliation table (15 rows) + canon note at top + ~25 inline `/ paper Eq (N)` annotations |
| `100_percent_adherence_backlog.md` | NEW top-of-file canon note (EN) + ~17 inline annotations across PT narrative and Python code snippets |
| `HVDM_OPTIMIZATION_PLAN.md` | NEW top-of-file note flagging `UncertaintyAwarePredictor`, `AMFCTrajectoryPredictor`, `AdvancedNeuralPredictor` as post-paper extension proposals |
| `repository_analysis.md` | **DELETED** (273 lines removed; superseded by `docs/VISION.md`) |

## Verification

- Re-grep for unannotated thesis-eq citations: 0 (only false-positive on
  the already-annotated `Equation 6.9 / paper §IV-A.1 narrative`).
- `dfg validate` / `dfg index --verify` / `dfg substrate check` /
  `make ci`: all OK.
- 2 pre-pr FAILs (ruff F401, pytest collection) are inherited carries
  from W1-1; W1-5 touches no Python and does not affect them.

## Closes

- `.dfg/agents/W1-5-docs-paper-canon-reconcile.md` (all 4 acceptance
  criteria — table inserted; PT preserved; HVDM extensions flagged;
  repository_analysis.md deleted)
- W1-5 in `.dfg/plan.yaml` (the second of two Group-1 units; **W1
  Group 1 now COMPLETE**)
