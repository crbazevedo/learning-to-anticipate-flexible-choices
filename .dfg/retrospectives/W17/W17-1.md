---
unit: W17-1
wave: W17
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  W17-1 H4 EDA produced EXACT match to thesis §7.2.3 p.145 "d = 87
  for FTSE" — 98 raw CSVs filter to exactly 87 thesis-faithful
  assets under the operator's continuous-trades criterion.

  Operationalized criterion (combining operator + thesis):
    1. no zero closes
    2. no NaN closes
    3. no negative closes
    4. no dissolution event
    5. first in-window trade within 5 business days of window start

  The 5-BD-slack tolerance was the key empirical calibration — too
  strict (0 BD) would drop 1-2 borderline assets; too loose (>10 BD)
  would let in 1-2 mid-window listings.

  Distribution was bimodal: 87 assets in "full window" (≥1500 rows),
  11 with much shorter presence (min 256 = ~1 trading year). Clean
  gap at (1400, 1500] rows confirms the cut is natural.

  Dropped 11 assets are documented with reasons:
    table (25): n_zero_close=1 (dissolution)
    table (28), (29), (32), (35), (37), (54), (61), (64), (74), (93):
      listed_mid_window (first_date 2007/2008/2011)

  data_loader.load_asset_data accepts new
  `enforce_thesis_continuous_trades: bool = False` kwarg; default
  unchanged for backward-compat with W12-W16 reports.

  8 tests shipped (artifact schema + filter behavior + dropped-asset
  exclusion). All green.

  PR #96 merged.
what_broke: |
  Initial criterion (just "no zero/NaN/negative closes") gave 97
  assets (vs 87 target) because mid-window listings were sneaking
  through (their pre-listing rows didn't exist in the CSV at all
  rather than being zero). Had to add the first-date proximity
  check explicitly. ~10 min debug.

  Result-of-truth artifact (asset_universe_87.json) is gitignored
  by default per W15-gate's .gitignore rule. Had to add a
  re-include exception. Clean fix via gitignore negation.
what_youd_change: |
  Could have spent 5 min upfront looking at the row-count
  distribution before committing to the criterion — would have
  surfaced the bimodality immediately and avoided the cycle-1
  97-asset result.

  The 5-BD slack tolerance is empirical; a thesis-citation
  justification would be cleaner. But thesis is silent on the
  exact tolerance, so empirical is honest.

  Should have also computed and committed the per-asset
  returns histogram + serial-autocorrelation diagnostics —
  useful for W18 if we end up debugging the algorithm signal
  (low-quality returns → noisy KF predictions). Deferred —
  premature instrumentation.
assumption_to_challenge: |
  Assumed: dropping the 11 polluting assets would IMPROVE the
  ASMS_mHDM_K3 vs SMS_RDM_K0 gap. CHALLENGE: W17-5 smoke proved
  this assumption WRONG — the gap actually widened from -5.53%
  to -8.72% on the clean data, while the std collapsed an order
  of magnitude.

  Interpretation: the 11 polluting assets had been MASKING the
  underlying algorithm signal (anticipatory chain underperforming
  baseline) with noise. Clean data exposes the signal more clearly.

  This is a positive scientific outcome — we now have a clean,
  thesis-faithful measurement of the ASMS vs SMS comparison — but
  it's a negative result for the W17 hypothesis "H4 closes the
  gap".

  Closes (BACKLOG): H4 with EXACT match to thesis d=87

  New carries: none (W17-1 closes H4 fully; the strategic
  implications fall under W17-5 carries)
---

# W17-1 — H4 asset universe EDA — EXACT match d=87 (BACKLOG H4)
