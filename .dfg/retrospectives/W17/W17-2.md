---
unit: W17-2
wave: W17
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  W17-2 wired record_kf_residual into the Kalman update path,
  closing W16-1-CARRY-1.

  Three coordinated edits:
   - kalman_filter.kalman_update: now RETURNS innovation^T @
     innovation scalar (was None). Additive; callers ignoring the
     return are unaffected.
   - sms_emoa._evaluate_solution: captures the returned scalar
     and calls self.anticipatory_learning.record_kf_residual(...)
     when a learner is attached.
   - AnticipatoryLearning._compute_lambda_k_with_branch: new method
     that also returns the branch indicator string for trace.

  Branch enum: {'k0_zero', 'warmup_traditional', 'kperiod_sum'}.
  Trace column `lambda_k_branch` added to CSV header (backward-
  compat: CSV readers tolerate extra trailing columns).

  8 tests shipped. 51/51 in combined W15+W16+W17-1+W17-2 thesis-spec
  suite.

  W17-5 wall-clock evidence (ASMS 48x slower than baseline) confirms
  the wiring is firing in production.

  PR #97 merged.
what_broke: |
  Pre-W17-2 the λ^K formula was correct on paper but silent in
  production (W16-1-CARRY-1 retro analysis). W17-2 makes it fire,
  but the W17-5 result shows firing it makes the gap WORSE
  (-5.53% → -8.72%). Possible interpretations:
    - λ^K saturating (too high) → over-correcting → ASMS over-
      anticipates → loses on OOS data
    - λ^K formula matches Eq 6.9 verbatim but Eq 6.9 itself may
      need re-reading for normalization scale
    - The (1/2) average per Eq 7.16 pulls λ_combined upward when
      λ^K > 0, making anticipation more aggressive than the
      thesis intends

  Filed for W18 ablation: re-run smoke with λ^K forced to 0
  (effectively rolling back W17-2) to see if W16-5's -5.53% gap
  is recovered.
what_youd_change: |
  The branch enum is a string; would be cleaner as a typed
  Literal or Enum. Stringly-typed because the CSV writer wants
  strings anyway and the consumer (trace assertions) does
  equality checks.

  The W17-5-CARRY-1 issue (per-worker flush no-op) would have
  been caught earlier if W17-2 had shipped an integration test
  that ran a full _evaluate_solution end-to-end with a mocked
  ExperimentManager flush. Adding only unit tests at the
  kalman/anticipatory layer left the integration gap hidden
  until W17-5 production run.

  Lesson: integration tests at the level of ExperimentManager.
  _run_algorithm would have surfaced the flush issue 30 min
  earlier.
assumption_to_challenge: |
  Assumed: wiring λ^K production firing would HELP close the gap.
  CHALLENGE: it widened the gap. Either the formula has a
  normalization bug (sigmoid scale may compress too aggressively
  on small residuals) or the (1/2) average with λ^H pulls
  anticipation too high.

  W18 ablation will decide:
    - run with λ_combined = λ^H only (W17-2 effectively rolled back)
    - run with λ_combined = 0.5*(λ^H + λ^K) (W17 current behavior)
    - compare gap deltas

  Closes (BACKLOG / carries):
   - W16-1-CARRY-1 fully (the wiring works; whether it HELPS is
     a separate question)

  New carries: none (W18 ablation will tell us if W17-2 needs
  revisiting; for now ship the structural correctness)
---

# W17-2 — record_kf_residual production wiring (W16-1-CARRY-1)
