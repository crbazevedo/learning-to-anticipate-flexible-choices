---
unit: W17-3
wave: W17
template: ADR-004-four-question
schema_version: 1.0.0
what_worked: |
  W17-3 threaded --lambda-trace-csv-path CLI arg through
  walk_forward_report.py to run_walk_forward (using the W16-4
  kwarg). 4 tests shipped covering CLI parsing + path propagation
  via mocks.

  PR #98 merged.
what_broke: |
  Despite unit-test green, the per-worker flush silently no-ops
  in production (W17-5-CARRY-1). The mocked tests verified path
  propagation up to run_walk_forward but didn't exercise the
  full ProcessPoolExecutor + ExperimentManager + algorithm
  reference chain.

  Mock-based unit tests caught the wiring contract but missed
  the production integration contract. Receipt of the
  "integration tests beat unit tests for catching inter-module
  API drift" pattern seen in W15-5 as well.
what_youd_change: |
  Should have included one foreground end-to-end test that
  actually runs walk_forward_report with --lambda-trace-csv-path
  set to a tmp_path and verifies CSV exists post-run. This would
  have caught the per-worker flush issue immediately.

  The cost would have been: ~30 sec test runtime (single seed,
  one period, minimal MC samples), vs ~2 hours of W17-5 / W18
  debugging.

  Lesson: when shipping wiring that crosses process boundaries
  (ProcessPoolExecutor in this case), the unit-test mock pattern
  is necessary-but-not-sufficient — pair with one foreground
  integration test.
assumption_to_challenge: |
  Assumed: passing the arg through the CLI + propagating to
  run_walk_forward via the W16-4 kwarg was sufficient for the
  CSV to emit. CHALLENGE: in production the flush hook in
  ExperimentManager._run_algorithm only fires when
  algorithm.anticipatory_learning is not None AND has the
  flush_lambda_trace_csv method. The ProcessPoolExecutor worker
  may be referencing algorithm differently than the central session.

  Possible fix paths (W18):
   1. Add a foreground integration test that asserts CSV exists
      post-walk_forward_report run
   2. Replace the try/except non-fatal warning with a structured
      log line that surfaces in stderr
   3. Add a per-worker debug flag that prints the algorithm
      reference state at flush time

  Closes (BACKLOG / carries):
   - W16-5-CARRY-1 / W16-4-CARRY-1 wiring done at CLI level

  New carries:
   - W17-5-CARRY-1 (NEW): per-worker flush production silent no-op
     (the wiring landed but doesn't produce the artifact end-to-end)
---

# W17-3 — --lambda-trace-csv-path driver wiring (W16-5-CARRY-1)
